[/Script/GameplayTags.GameplayTagsList]

; ─────────────────────────────────────────────────────────────
; Hub.Zone — granted by AAFLHubZoneVolume (server-auth infinite
; GE) while the pawn stands in that zone. Drives per-zone net
; cull (UAFLHubNetProfileComponent), zone prompts, chat scope.
; Zones map to IRONICS_LOBBY_UX_FLOW_SSOT nodes N4/N4a-N4h.
; Never grants or removes fire authorisation (SSOT s2.3).
; ─────────────────────────────────────────────────────────────
GameplayTagList=(Tag="Hub.Zone.Main",DevComment="Outpost Earth spawn plaza / open base (flow N4). Default zone -- no volume required")
GameplayTagList=(Tag="Hub.Zone.Range",DevComment="Shooting Range DOOR area in the hub (flow N4f). The range itself is a separate map with its own experience")
GameplayTagList=(Tag="Hub.Zone.PX",DevComment="PX Store interior (flow N4g)")
GameplayTagList=(Tag="Hub.Zone.Labs",DevComment="Robo Labs -- creator station (flow N4a, Track C)")
GameplayTagList=(Tag="Hub.Zone.Barracks",DevComment="Loadout Barracks interior (flow N4d)")
GameplayTagList=(Tag="Hub.Zone.Cafe",DevComment="Patriot Cafe social club interior (flow N4b)")
GameplayTagList=(Tag="Hub.Zone.EMLounge",DevComment="EM Lounge social club interior (flow N4c)")
GameplayTagList=(Tag="Hub.Zone.Deploy",DevComment="Deployments threshold -- League/Staked door (flow N4h)")
GameplayTagList=(Tag="Hub.Zone.Tournament",DevComment="Tournaments/Challenges tent (flow N4e -> N5)")
GameplayTagList=(Tag="Hub.Zone.MiniGame",DevComment="Mini Games tent (flow N4e -> N5)")
GameplayTagList=(Tag="Hub.Zone.Assigned",DevComment="Assigned Match door (flow N7 entry)")

; ─────────────────────────────────────────────────────────────
; Hub.Restriction — fail-closed safety. NoFire is granted by an
; infinite GE on possession in the HUB EXPERIENCE only; every AFL
; fire ability carries it in ActivationBlockedTags (AFL-3014).
; No volume ever removes it -- fire authorisation is by
; EXPERIENCE (the Range map's experience simply never applies it).
; ─────────────────────────────────────────────────────────────
GameplayTagList=(Tag="Hub.Restriction.NoFire",DevComment="Blocks every AFL fire ability while in the hub (SSOT s2.3, fail-closed; flow N4 'NO FIRE except authorised zones')")
