// Copyright C12 AI Gaming. AFL / BAG MAN.
// NOTE: Reconstructed at placement time -- this file (the module impl) was specified in
// the stub README but omitted from the shipped loose-file stub set. Content is the
// canonical form supplied by the author; a content-light runtime module has exactly one
// correct shape here. FAFLVFXModule (named, AFL convention) gives StartupModule/
// ShutdownModule a home for later cue-path registration (GameplayCueNotifyPaths).
#pragma once

#include "Modules/ModuleManager.h"

/** AFL laser/beam cosmetic module. Holds GameplayCue notifies + the visual-provider interface. */
class FAFLVFXModule : public IModuleInterface
{
};
