# Bag_Man handoff — 2026-05-20 EOD (transferring to UE+terminal)

Picking this up in the UE AIK panel or a fresh Claude Code terminal? Read this first.

## Step 0 — Before anything else

**Push the unflushed local commit.** At session end, `5cf71838 ci: mark AFL-0109 merged` exists locally but never made it to origin because github.com was hard-down (5-min TLS handshake timeouts on every attempt).

```powershell
cd C:\Dev\Bag_Man
git status                 # should show: ahead by 1, behind by 1 (the PR #9 merge commit)
git pull --rebase origin main   # may conflict on queue.yaml AFL-0109 status: keep 'merged'
git push origin main       # flushes 5cf71838 to origin
```

If conflict on queue.yaml AFL-0109 status line: keep `merged` (our local), reject `running` (origin's stale orchestrator write that's always swept in by `git add -A`). It's the same one-line conflict pattern every yolo PR has produced.

If `git pull` still hits a network wall: probe with `claude -p "Say OK" --output-format stream-json --verbose 2>&1 | Select-Object -First 5` (10s) to see if Anthropic is up. If both git and Anthropic are blocked, this is a wifi/ISP issue — wait 10-15 min and retry.

## Where we are right now

- **8 of 13 yolo-queue tasks merged** (AFL-0104 through AFL-0109 + AFL-0214). 5 pending: AFL-0110 (repointed), AFL-0111, AFL-0211, AFL-0213, AFL-0218.
- **Sprint 3 Phase 2 still NOT closed** — AFL-0304-Bi blocker open. AFL-0214's UAFLHeroComponent is the candidate fix to test.
- **Orchestrator is production-grade**: stream-json + 10-min stall watchdog + push retry + self-healing labels + rate-limit-event detection all validated under load this session.
- **Filename note**: `BAG_MAN_LIVE_TRACKER_v2_2.html` is the SSOT, internal version v2.3 (filename not yet renamed).

## What shipped 2026-05-20 (this session)

### 1. AFL-0215 standalone lint — commit `cda45f34`

Closed the false-merge gap from bootstrap commit `4945a01a`. AFL-0215 had been marked merged in queue.yaml but the actual deliverable (a standalone Python lint script) never existed; rules only lived inline in `afl-yolo-pr.yml` (yolo-PRs only).

**Files now on main:**
- `Tools/AFL_Yolo/lint_afl_0215.py` — standalone CLI (~230 lines), three regexes byte-identical with `orchestrator.py LINT_RULES`. Supports `--self-test` regression guard (writes 3 fixture files, asserts each rule fires its own fixture).
- `.github/workflows/afl-yolo-pr.yml` — three new steps before Compile: self-test → standalone scan → kept inline-legacy as belt-and-braces (will retire in followup).

**Verified live:** retroactive scan against AFLCombat + AFLCore + AFLMovement plugin trees (45 files): **clean**. All 7 previously-merged tasks honored the three rails.

**Open followup:** wire the same standalone into `.github/workflows/afl-pr-build.yml` so non-yolo PRs get the gate too. Requires `fetch-depth: 0` on that workflow's checkout — small, separate commit.

### 2. AFL-0109 asset naming validator — PR #9 (merged), local cleanup `5cf71838` (pending push)

`Tools/AFL_Lint/asset_naming.py` + `afl-naming-lint` job in `afl-pr-build.yml`. End-to-end orchestrator run: spawn → Claude session 4m08s → push (retry 1 hit 5-min TLS timeout, retry 2 succeeded after 30s backoff) → PR open → CI green → `gh pr merge --squash --auto` → merged. Total ~28 min.

**First production firing of the push retry from `ba4f90d4`.** Recovered from a connection timeout that would have manually killed the task pre-2026-05-18.

**afl-naming-lint passed on its own PR** in 13s. The validator IS the deliverable working AS the deliverable. Self-validating.

## What's blocked / open / next

### Track A — AFL-0304-Bi UE PIE test (Sprint 3 Phase 2 closeout)

**Hypothesis:** AFL-0214's `UAFLHeroComponent` (extends `ULyraHeroComponent`) is the missing piece causing the Lyra possession chain inconsistency under `B_AFL_Test_Experience`. Currently `PawnExtension` initializes but `LyraPlayerController.GetPawn()` returns null → EnhancedInput has nowhere to route.

**Action:** wire `UAFLHeroComponent` into the canonical `DA_AFL_PawnData_Hero_Default` actor component list (or the ActionSet referenced by `B_AFL_Test_Experience`), run PIE on `L_AFL_Movement_Validate`, verify acceptance criteria.

**Acceptance:**
- `PC.GetPawn() == AAFLCharacter`
- WASD/mouse reach the pawn
- LeftShift triggers full dash loop
- Second LeftShift inside 3s blocked by cooldown

**If green:** tag `sprint-3-phase-2-cmc-complete`. **Huge** — closes a multi-day blocker.

**Network independence:** zero. PIE is purely local. Only step that needs network is the final commit + push if you make changes.

This is the highest-leverage work right now. Best suited to the UE AIK panel session (which has editor-side access).

### Track B — AFL-0111 (Sprint 1 closeout, smallest task)

QA test plan template. S-estimate. Pure docs work, no compile, `verify: none`.

```powershell
python C:\Dev\Bag_Man\Tools\AFL_Yolo\orchestrator.py -v task AFL-0111
```

Network-dependent (Claude Code's Anthropic API + git push). Probe rate window first (~10s test).

### Track C — Sprint 2 tasks now runnable

- **AFL-0211** UAFLLagCompensationWorldSubsystem (XL, eng). Builds the lag comp foundation Sprint 2 hitscan validation needs. Most ambitious.
- **AFL-0213** Telemetry hook real impl (M, eng). AFL-0106 already shipped the stub — this finishes it. Plays well with AFL-0211 since both are server-side hit-validation infra.
- **AFL-0218** Skill registry CI check (S, devops). Smallest. Pure CI tooling.

## Files / paths

| Path | What |
|---|---|
| `BAG_MAN_LIVE_TRACKER_v2_2.html` | SSOT tracker, internal v2.3. Amendments banner has the running narrative. |
| `HANDOFF_2026-05-18.md` | Previous session handoff (still relevant, esp. gotchas list). |
| `HANDOFF_2026-05-20.md` | This document. |
| `Docs/BAG_MAN_MASTER_BUILD_v2.0.md` | Master spec. Sprint structure, hard rails, architectural decisions. |
| `Docs/AFL_YOLO_PLAYBOOK.md` | Orchestrator operating manual. |
| `Tools/AFL_Yolo/orchestrator.py` | The orchestrator (~1200 lines). Read top docstring + § headers. |
| `Tools/AFL_Yolo/queue.yaml` | Task queue. Edit it before any task that needs scope/dep/autonomy changes (e.g., AFL-0110 still has its old greybox brief). |
| `Tools/AFL_Yolo/config.toml` | Live Discord webhook + paths. **Gitignored.** Don't commit. |
| `Tools/AFL_Yolo/lint_afl_0215.py` | New today. The standalone architectural lint. Tells the orchestrator's existing LINT_RULES exists in another place too — keep them byte-identical. |
| `Tools/AFL_Lint/asset_naming.py` | New today (AFL-0109). The asset prefix validator. |
| `Tools/AFL_Yolo/.state/claude_code_live.log` | Per-spawn human-readable Claude event summary. Tail with `Get-Content -Wait`. |
| `Tools/AFL_Yolo/.state/claude_code_live.jsonl` | Per-spawn raw JSON from Claude. For deep diagnosis. |
| `Tools/AFL_Yolo/.state/orchestrator.log` | Orchestrator's own log. Always available. |

## Gotchas (additions to the 2026-05-18 list)

1. **github.com network can go hard-down on this box** (2026-05-20 evening confirmed). 5-min TLS handshake timeouts. `gh` API may go down at a different time than `git`. Probe both before assuming.
2. **`http.version HTTP/1.1` is already globally set** — was set by a prior session as the documented HTTP/2 multiplexing workaround. Doesn't help with the worse outages, but no need to re-apply.
3. **`http.postBuffer = 524288000`** also already set. Don't try to "fix" these network configs without checking — they're already at the resilient settings.
4. **The orchestrator's `git add -A` sweeps queue.yaml mid-task writes into the yolo PR.** Every PR ships with stale `status: running` for its own task. The `ci: mark AFL-XXXX merged` cleanup commit pattern is the standing remedy. Not yet fixed at source.
5. **`gh pr merge --squash --auto` is the working substitute for phone-merge** when both your phone and git are flaky but `gh` API works. Collapses to immediate merge if all required checks are green at submission time.
6. **The empty `Plugins/GameFeatures/AFLMovement/Content/Components/` folder we kept around for 2 days finally tripped the orchestrator's `ensure_clean_tree` pre-flight on 2026-05-20.** I deleted it. If you need it back for Sprint 3 staging, add a `.gitkeep` so git tracks it and the orchestrator doesn't halt.

## Two-Claude-sessions coordination notes (UE AIK panel + terminal)

You mentioned running both. Some honest observations:

- **No session-resume bridge exists between separate Claude Code instances.** Each session is independent. Memory (`~/.claude/projects/C--Dev-Bag-Man/memory/`) is the shared state mechanism — it persists, both sessions can read it. Git is the other shared state.
- **Conversation context does NOT transfer.** Anything I learn in this session that isn't written to memory or the repo is lost. The handoff doc and the new memory entries are how the next session "knows" what we did.
- **Concurrency**: if both Claudes edit the same file or both `git commit && push`, you get conflicts. The risk is real even if low-probability. Rule of thumb: keep one Claude on UE-side (PIE, editor inspection, BP-side edits) and the other on git/Python/orchestrator. Or just toggle which one you're actively driving.

## Suggested resume sequence

1. **Flush the pending push** (Step 0 above). Confirm via `git log --oneline -3` that `5cf71838` is on origin.
2. **Probe rate window**: `claude -p "Say OK" --output-format stream-json --verbose 2>&1 | Select-Object -First 5`. Check `overageStatus`. If healthy, orchestrator is safe to use.
3. **Decide direction**: AFL-0304-Bi UE test (highest leverage, network-independent, UE-side work) OR AFL-0111 (cheapest yolo task, network-dependent).
4. **For AFL-0304-Bi**: open UE Editor, add UAFLHeroComponent to the canonical PawnData (via DA_AFL_PawnData_Hero_Default or the ActionSet), PIE on L_AFL_Movement_Validate, verify acceptance. If green: commit + push + tag.
5. **For AFL-0111**: `python Tools/AFL_Yolo/orchestrator.py -v task AFL-0111`. Stream-json + watchdog will catch hangs in 10 min. `gh pr merge N --squash --auto` substitutes for phone merge when needed.

---

*Generated 2026-05-20 evening end-of-session. This session: 2 commits + 1 PR merged. Orchestrator hardening fully validated under production load. Next session picks up from a clean 8-merged / 5-pending state once 5cf71838 is flushed.*
